
dependencies {
    api group: 'org.mongodb', name: 'mongodb-driver-core', version: "${mongoDriverVersion}"
    api group: 'org.mongodb', name: 'mongodb-driver-sync', version: "${mongoDriverVersion}"

    testImplementation group: 'de.bwaldvogel', name: 'mongo-java-server', version: '1.47.0'
}
